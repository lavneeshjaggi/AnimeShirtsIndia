{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/(login|logout|register|authenticated)", "dest": "server.js" },
    { "src": "/favicon.ico", "dest": "/client/dist/favicon.ico" },
    { "src": "/.*", "dest": "server.js" }
  ]
}
